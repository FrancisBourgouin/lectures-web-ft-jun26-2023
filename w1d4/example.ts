let bob: number = 5;

bob = 10;

interface url {
  longUrl: String;
  shortUrl: String;
  id: Number;
}

// shortUrl:longUrl

let someUrl: url = {
  longUrl: "...",
  shortUrl: "...",
  id: 0,
};

// function to update

someUrl.longUrl = "https://bing.com";

// someUrl.
